{
  "name": "rota",
  "version": "0.1.0",
  "private": true,
  "description": "USDC payments on Arc",
  "license": "MIT",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build:web": "npm run build --prefix web",
    "build:contracts": "npm run build --prefix contracts",
    "probe": "npm run probe:testnet --prefix contracts",
    "probe:mainnet": "npm run probe:mainnet --prefix contracts",
    "install:all": "npm install --prefix contracts && npm install --prefix web"
  }
}
