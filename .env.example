# ---- contracts (hardhat) ----

# TESTNET deployer key. Used by arcTestnet only.
PRIVATE_KEY=

# MAINNET deployer key. Used by arcMainnet only, and deliberately a separate
# variable: a testnet key has usually been pasted into faucets, scripts and
# chat windows, and must never be the key holding real money. The deploy
# script refuses to touch mainnet unless this is set.
MAINNET_PRIVATE_KEY=

# Arc mainnet. Not hardcoded anywhere in the repo; arcMainnet is only
# registered as a Hardhat network once both of these are set.
ARC_MAINNET_CHAIN_ID=5042
ARC_MAINNET_RPC_URL=https://arc-mainnet.g.alchemy.com/v2/YOUR_KEY
# fallback: https://rpc.mainnet.arc.io

# Optional: read USDC balance for this address instead of deriving it from
# PRIVATE_KEY when running scripts/probe.ts
ADDRESS=

# ---- web (next.js) ----
# Copy into web/.env.local. See web/.env.example for the deployed addresses.
NEXT_PUBLIC_ARC_MAINNET_CHAIN_ID=5042
NEXT_PUBLIC_ARC_MAINNET_RPC_URL=https://arc-mainnet.g.alchemy.com/v2/YOUR_KEY

# ---- Sign-in (Circle user-controlled wallets), server-side only ----

# Circle Console > Project Settings > API Keys. SERVER ONLY.
# This key can create challenges for every user in the project. It must never
# be prefixed NEXT_PUBLIC_ and must never reach a browser. It cannot approve a
# challenge — only the member can — but it must still be treated as a secret.
CIRCLE_API_KEY=
